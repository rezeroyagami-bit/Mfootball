const c=document.getElementById('pitch'),x=c.getContext('2d');let W,H,run=false,t=90,score=[0,0],keys={},last=0;const p={x:.25,y:.5,r:16,spd:.00055},ball={x:.5,y:.5,r:8,vx:0,vy:0};const ai={x:.72,y:.5,r:16};function resize(){W=c.clientWidth;H=c.clientHeight;c.width=W*devicePixelRatio;c.height=H*devicePixelRatio;x.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0)}addEventListener('resize',resize);resize();function pos(o){return{x:o.x*W,y:o.y*H}}function draw(){x.clearRect(0,0,W,H);x.fillStyle='#168443';x.fillRect(0,0,W,H);x.strokeStyle='#dff5df';x.lineWidth=2;x.strokeRect(3,3,W-6,H-6);x.beginPath();x.moveTo(W/2,0);x.lineTo(W/2,H);x.stroke();x.beginPath();x.arc(W/2,H/2,Math.min(W,H)*.12,0,Math.PI*2);x.stroke();x.strokeRect(4,H*.32,35,H*.36);x.strokeRect(W-39,H*.32,35,H*.36);x.fillStyle='#fff';x.beginPath();let b=pos(ball);x.arc(b.x,b.y,ball.r,0,7);x.fill();let q=pos(p);x.fillStyle='#0877d1';x.beginPath();x.arc(q.x,q.y,p.r,0,7);x.fill();let a=pos(ai);x.fillStyle='#d62b35';x.beginPath();x.arc(a.x,a.y,ai.r,0,7);x.fill()}function move(dt){let dx=(keys.right?1:0)-(keys.left?1:0),dy=(keys.down?1:0)-(keys.up?1:0),l=Math.hypot(dx,dy)||1;p.x+=dx/l*p.spd*dt;p.y+=dy/l*p.spd*dt;p.x=Math.max(.04,Math.min(.96,p.x));p.y=Math.max(.05,Math.min(.95,p.y));let d=Math.hypot(ball.x-p.x,ball.y-p.y);if(d<.045){ball.vx+=(p.x-ball.x)*.002;ball.vy+=(p.y-ball.y)*.002}ball.x+=ball.vx*dt;ball.y+=ball.vy*dt;ball.vx*=Math.pow(.985,dt);ball.vy*=Math.pow(.985,dt);if(ball.x<.02||ball.x>.98)ball.vx*=-.8;if(ball.y<.02||ball.y>.98)ball.vy*=-.8;ai.x+=(ball.x-ai.x)*.00025*dt;ai.y+=(ball.y-ai.y)*.00025*dt;if(Math.hypot(ball.x-ai.x,ball.y-ai.y)<.045){ball.vx+=(ball.x-ai.x)*.002;ball.vy+=(ball.y-ai.y)*.002}if(ball.x>.985){score[0]++;resetBall()}if(ball.x<.015){score[1]++;resetBall()}}function resetBall(){ball.x=.5;ball.y=.5;ball.vx=ball.vy=0}function tick(now){let dt=Math.min(40,now-last||16);last=now;if(run){t-=dt/1000;if(t<=0){t=0;run=false;document.getElementById('start').classList.remove('hidden');document.getElementById('start').textContent='إعادة المباراة'}move(dt);document.getElementById('score').textContent=score.join(' - ');document.getElementById('time').textContent=`${String(Math.floor(t/60)).padStart(2,'0')}:${String(Math.floor(t%60)).padStart(2,'0')}`}draw();requestAnimationFrame(tick)}requestAnimationFrame(tick);document.getElementById('start').onclick=()=>{score=[0,0];t=90;resetBall();run=true;document.getElementById('start').classList.add('hidden')};document.querySelectorAll('[data-key]').forEach(b=>{let k=b.dataset.key;['touchstart','mousedown'].forEach(e=>b.addEventListener(e,z=>{z.preventDefault();keys[k]=1}));['touchend','mouseup','mouseleave'].forEach(e=>b.addEventListener(e,z=>{z.preventDefault();keys[k]=0}))});document.getElementById('shoot').onclick=()=>{if(Math.hypot(ball.x-p.x,ball.y-p.y)<.07){ball.vx=.012;ball.vy=(ball.y-.5)*.006}};document.getElementById('pass').onclick=()=>{if(Math.hypot(ball.x-p.x,ball.y-p.y)<.07){ball.vx=.007;ball.vy=(ball.y-ai.y)*.004}};